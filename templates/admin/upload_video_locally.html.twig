{% extends 'admin/base.html.twig' %}

{% block main_admin %}
    <h2>Upload a video</h2>

    {{ form_start(form) }}
        <div class="form-group">
            <div class="custom-file">
                {{ form_row(form.uploadVideo) }}
            </div>
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
   {{ form_end(form) }}
{% endblock %}

